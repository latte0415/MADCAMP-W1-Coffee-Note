[적용 중]

# creation (1-1-0): 생성 팝업 기본 화면
## 구성요소
- 뒤로가기 버튼 -> 이전 화면
- 이미지 추가하기 -> 이미지 업로드
- 텍스트 입력창 (메뉴명, 카페명, 날짜명, 한줄평)
- 슬라이더 (산미, 바디, 쓴맛): 1~10 단위
- 별점 입력: 이미지, CSS에는 없으나 기존 구현안 참고하여 사용할 것. (별 5개 클릭하여 입력하는 형태)
- 상세정보 추가하기 체크 박스: 체크할 경우, 아래가 스크롤할 수 있게 확장됨. 자세한 내용은 creation_spread, creation_spread_2 참고
- 기록하기: 입력한 정보를 바탕으로 createNote

# creation_spread (1-1-0): 생성 팝업에서 상세 정보 추가하기 버튼 눌렀을 때
## 구성요소
- creation (1-1-0)과 동일. spread 하면 추가되는 요소는 creation_spread_2 참고

# creation_spread_2 (1-1-0): 생성 팝업에서 상세 정보 추가하기 버튼 눌렀을 때 (스크롤 하단)
## 구성요소 (상기 언급한 요소 제외하고)
- AI 자동생성 버튼: 현재는 구현하지 않음. 클릭하면 NotImplemented 뜨게 할 것
- 텍스트 입력창: 국가/지역, 품종
- 텍스트 입력창 + 드롭다운: 드롭다운에서 선택하면 해당 내용이 텍스트 입력창에 자동 추가됨. 직접 입력을 선택할 경우, 내부적으로는 ETC로 처리하며, 사용자가 텍스트 입력창에 내용 입력할 수 있게 함. (텍스트 입력창 -> *_text, 드롭다운 -> *)
- 테이스팅 노트: 쉼표 또는 띄어쓰기 단위로 해시태그 생성 (최대 5개)
- 기록하기 버튼: 입력한 정보를 바탕으로 createNote + createDetail

# library_folded (1-0-0): 라이브러리 화면 (상세 필터 꺼짐)
## 구성요소
- 검색창: 텍스트 입력 (search 서비스 활용)
- 상세필터 폴드: 클릭하면 펼쳐져서 library_spread로 전환
- 정렬 버튼: 최신순, 별점순에 맞게 정렬되도록 함.
- 컴포넌트
  - basic 컴포넌트: 카페명, 마신 날짜, 메뉴명, 한줄평이 왼쪽에 위치하고, 오른쪽에 산미, 바디 쓴맛 존재
  - detail_included 컴포넌트: 기존 basic 컴포넌트와 동일하되, 메뉴명과 한줄평 사이에 국가/지역, 품종, 가공방식, 로스팅포인트, 추출방식 중 NULL이 아닌 것을 모아서 디스플레이. 해시태그는 오른쪽 하단에 나열
- 기록하기 버튼: creation (1-1-0) 팝업 호출
- 하단 내비게이션 바: 라이브러리, 갤러리, AI 가이드 간 탭 전환

# library_spread (1-0-0): 라이브러리 화면 (상세 필터 켜짐)
library_folded와 공통된 것은 생략함
## 구성요소
- 상세 필터: 산미, 바디, 쓴맛에 대해서 1~10 슬라이더 기능. similarlity 관련 서비스 사용하여 필터링


----
[적용 X]

# details (1-2-0)
따로 png, css는 존재 하지 않음. creation (1-1-0)과 최대한 유사한 형태로 구현할 것

# gallery (2-0-0)
## 구성 요소
- 기록하기 버튼 및 하단 내비게이션 바: library (1-0-0)과 동일
- 갤러리
  - 이미지 컴포넌트 한 행당 2개.
  - 최신순 정렬
  - 이미지 없는 note는 보여주지 않음 (기존 코드와 동일)
  - 이미지 컴포넌트
    - 한 번 클릭하면 불투명한 검정색이 사진을 덮고, 정보가 보여짐 (활성화 상태)
    - 디스플레이되는 정보: 메뉴명, 산미, 바디, 쓴맛, 카페명, 마신 날짜
    - 활성화 상태에서 클릭하면, 상세페이지로 이동 -> details (1-2-0)
    - 다른 컴포넌트를 클릭해서, 타 컴포넌트가 활성화되면 기존 활성화 컴포넌트는 비활성화 (즉, 최대 1개 컴포넌트만 활성화 가능)

# ai_guide (3-0-0)
## 구성 요소
- 입력창: 사용자의 자연어 텍스트 입력창
- 가이드 받기: 관련 서비스 호출 예정 (현재는 Not implemented 처리할 것.)
- AI가 생성한 정보
  - 국가/지역, 품종, 가공방식, 로스팅포인트, 추출방식, 테이스팅 노트 -> creation과 유사한 형태
  - 센서리 가이드: 2~3줄 내외의 텍스트
- 이어서 기록하기 버튼: 센서리 가이드를 제외한, 타 정보를 creation pop up에서 자동으로 채우면서 팝업 호출.
## 참고사항
현재는 dummy data로 구현할 것